[package]
name = "ai-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
# General utilities
anyhow = "1"


# Hashing
sha2 = "0.10"

# text extraction
pdf-extract = "0.9.0"

# open ai client
async-openai = "0.23"
tokio = {version = "1", features = ["full"]}
dotenv = "0.15"

